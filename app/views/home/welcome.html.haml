#block-text.block
  .content
    %h2.title= @org_name
    .inner
      %p.first
        New company/organization wellcome page.
        First page :)))
- content_for :sidebar, render(:partial => 'sidebar')
